import React from "react";

const TemplateDetailLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className=" z-200 ">{children}</div>;
};

export default TemplateDetailLayout;
