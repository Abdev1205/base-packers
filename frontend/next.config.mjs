/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn-icons-png.flaticon.com', 'images.unsplash.com', 'i.postimg.cc', 'plus.unsplash.com', 'img.clerk.com'],
  },
};

export default nextConfig;
